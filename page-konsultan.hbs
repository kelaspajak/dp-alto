{{!< default}}

<style>
    .author-grid {
        display: grid;
        grid-template-columns: repeat(1, minmax(0, 1fr)); /* Default: 1 column */
        gap: 20px;
    }

    @media (min-width: 40rem) {
        .author-grid {
            grid-template-columns: repeat(2, minmax(0, 1fr)); /* >= 40rem: 2 columns */
        }
    }

    @media (min-width: 48rem) {
        .author-grid {
            grid-template-columns: repeat(3, minmax(0, 1fr)); /* >= 48rem: 3 columns */
        }
    }

    .author-card {
        border: 1px solid #eee;
        padding: 20px;
        text-align: center;
    }
    .author-image {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        object-fit: cover;
    }
</style>

<div class="content-area">
    <main class="site-main gh-outer">
        <article class="gh-article post">
            <header class="gh-article-header">
                <h1 class="gh-article-title">Our Consultants</h1>
            </header>
            <div class="gh-article-content">
                {{#get 'authors' include='count.posts'}}
                    <div class="author-grid">
                        {{#foreach authors}}
                            {{> "author-card" }}
                        {{/foreach}}
                    </div>
                {{/get}}
            </div>
        </article>
    </main>
</div>